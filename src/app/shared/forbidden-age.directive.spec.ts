import { ForbiddenAgeDirective } from './forbidden-age.directive';

describe('ForbiddenAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenAgeDirective();
    expect(directive).toBeTruthy();
  });
});

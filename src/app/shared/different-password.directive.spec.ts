import { DifferentPasswordDirective } from './different-password.directive';

describe('DifferentPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new DifferentPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
